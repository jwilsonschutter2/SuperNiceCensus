/** Friendly-name CSV and GeoJSON exports. */
function selectedExportGeoids(){return window.PrettyCensusSelectedGeoids instanceof Set&&window.PrettyCensusSelectedGeoids.size?window.PrettyCensusSelectedGeoids:null}
function filterFeatureCollectionToChosenGeography(fc,level=geoLevel){if(!fc||fc.type!=="FeatureCollection")return fc;const selected=selectedExportGeoids();let features=fc.features||[];if(selected)features=features.filter(feature=>selected.has(PrettyCensusGeoid.fromFeature(feature,level)||String(feature.properties?.__geoid||"")));else if(level==="tract"&&selectedTract&&selectedTract!=="*")features=features.filter(feature=>(PrettyCensusGeoid.fromFeature(feature,level)||String(feature.properties?.__geoid||"")).slice(5,11)===String(selectedTract).padStart(6,"0"));else if(level==="blockgroup"){if(selectedTract&&selectedTract!=="*")features=features.filter(feature=>(PrettyCensusGeoid.fromFeature(feature,level)||String(feature.properties?.__geoid||"")).slice(5,11)===String(selectedTract).padStart(6,"0"));if(selectedBlockGroup&&selectedBlockGroup!=="*")features=features.filter(feature=>(PrettyCensusGeoid.fromFeature(feature,level)||String(feature.properties?.__geoid||"")).slice(11,12)===String(selectedBlockGroup));}return{...fc,features}}
function compactFeatureCollection(fc,keepProperties=[]){const keep=new Set(keepProperties);return{...fc,features:(fc.features||[]).map(feature=>({...feature,properties:Object.fromEntries(Object.entries(feature.properties||{}).filter(([key])=>keep.has(key)))}))}}

function currentRowsAsObjects(){return apiArrayToObjects(currentFetchedData)}function suggestCurrentCsvName(){return `census_${selectedDataset.replaceAll('/','_')}_${selectedYear}_${geoLevel}.csv`}function exportHeader(h){return variableMeta(h).exportName||h}function cleanExportValue(v){if(v===null||v===undefined||v==="")return v;const n=Number(v);return Number.isFinite(n)&&n<0?0:v}function friendlyExportRow(r){return Object.fromEntries(Object.entries(r).map(([k,v])=>[exportHeader(k),cleanExportValue(v)]))}function exportRowsToCsv(rows,filename){if(!rows?.length)return;const clean=rows.map(friendlyExportRow),headers=Array.from(new Set(clean.flatMap(Object.keys))),csv=[headers.map(csvEscape).join(","),...clean.map(r=>headers.map(h=>csvEscape(r[h])).join(","))].join("\n"),b=new Blob([csv],{type:"text/csv;charset=utf-8"}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download=filename;a.click();URL.revokeObjectURL(a.href)}function friendlyGeoJson(fc){return{...fc,features:fc.features.map(f=>({...f,properties:friendlyExportRow(f.properties||{})}))}}function downloadGeoJson(fc,filename){if(fc?.type!=="FeatureCollection")return;const b=new Blob([JSON.stringify(friendlyGeoJson(fc),null,2)],{type:"application/geo+json"}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download=filename;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),0)}
